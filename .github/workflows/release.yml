# 0044 / 0069 — draft GitHub Release (DISABLED until Maxim go).
# When enabled: tag MUST be `1.0.0+N` (or `v1.0.0+N`) and upload the
# platform-signed APK as `zee-power-toys.apk` (fallback name `app-release.apk`)
# so in-app self-update can find it anonymously.
name: release
on:
  workflow_dispatch:
jobs:
  release:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Enable after Maxim go — publish zee-power-toys.apk on tag 1.0.0+N"
