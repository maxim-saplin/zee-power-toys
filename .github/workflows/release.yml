# 0044 — draft GitHub Release (DISABLED until Maxim go). Use ci-publish-prep for CI.
name: release
on:
  workflow_dispatch:
jobs:
  release:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Enable after Maxim go"
