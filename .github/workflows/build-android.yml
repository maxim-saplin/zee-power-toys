# 0044 draft — disabled until Maxim's explicit go (flip `if` / remove gate).
name: build-android

on:
  workflow_dispatch:
  # push:
  #   tags: ['v*']

jobs:
  apk:
    if: false  # DISABLED — enable after 0044 go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: flutter analyze
      - run: flutter test
      - run: flutter build apk --release
      - uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk
